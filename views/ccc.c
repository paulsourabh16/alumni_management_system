#include<stdio.h>
#include<conio.h>
int main()
{
    int u,v,a,b;
    printf("Enter the number of vehicles:");
    scanf("%d",&u);
    printf("Enter the number of wheels:");
    scanf("%d",&v);
    

    printf("Total number of two wheelers are: %d",a);
    printf("Total number of four wheelers are: %d",b);
}